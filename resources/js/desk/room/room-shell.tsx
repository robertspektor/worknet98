/* The wall the machine stands against. The camera sits low at the desk, so
   it is the only surface of the flat left in the picture. */

export function RoomShell() {
    return (
        <div className="room-shell" aria-hidden="true">
            <span className="shell-back" />
        </div>
    );
}
